export interface notiData {
    preferenceNoti: boolean;
    amountNoti: boolean;
  }

  export interface amountData {
    amountNoti: boolean;
  }
  export interface prefData {
    preferenceNoti: boolean;
  }